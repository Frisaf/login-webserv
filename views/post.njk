{% extends "layout.njk" %}

{% block content %}

<main class="wrapper">
    <article>
        <h2>{{ post.title }}</h2>
        <p>{{ post.content }}</p>
        <footer>
            <p>Posted by <strong>{{ post.name }}</strong> at {{ post.created_at }}</p>
        </footer>
    </article>
    <p><a href="/posts/{{ post.id }}/delete">Delete post</a></p>
</main>

{% endblock %}